hostname: neptune
logging:
  # logfile: "meerkat.log"
  loglevel: debug
database:
  host: 10.0.0.8
  user: tsdb
  password: tsdb
  database: tsdb
binaries:
  df: "df"
  free: "free"
  pidstat: "pidstat"
